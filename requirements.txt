Flask>=2.0
Flask-SQLAlchemy>=3.0
Flask-Migrate>=4.0
Flask-JWT-Extended>=4.0
python-dotenv>=1.0
gunicorn>=20.0
psycopg2-binary>=2.9  # optional for Postgres

# Websocket / realtime
Flask-SocketIO>=5.0
python-socketio>=5.0
python-engineio>=4.0
eventlet>=0.33  # or gevent if preferred

# embeddings / vector store
sentence-transformers>=2.2.2

# AWS / HTTP
boto3>=1.26
requests>=2.0

# Optional: useful for RAG implementations (vector DBs / ML)
faiss-cpu==1.12.0

# File reading
PyPDF2>=3.0
python-docx>=0.8.11
# optional: textract for legacy .doc support (install if needed)
# textract>=1.6.5
